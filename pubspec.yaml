name: nominatim_flutter
description: Nominatim Flutter Plugin offers Nominatim integration for geocoding in Flutter with async loading and Hive caching.
version: 0.0.2
homepage: https://github.com/binhlam10796/nominatim_flutter

environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.2
  dio_cache_interceptor: ^3.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: